/**
 * this is how I establish what a Pokemon is on a global scope.
 */
export type Pokemon = {
    id: number,
    name: string,
    image_src: string,
    type: string,
    description: string,
    notes: string,
}